# Enable the H2 database (file-based for permanent storage)
spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:file:./data/ufitdb
spring.datasource.driverClassName=org.h2.Driver

# Automatically create/update the database tables based on your ufit.java model
spring.jpa.hibernate.ddl-auto=update